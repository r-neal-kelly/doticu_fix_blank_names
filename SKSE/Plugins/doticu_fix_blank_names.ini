; Settings for doticu_fix_blank_names

; min == 3, max == 30, default == 5
fix_interval_in_seconds: 5

; 0 == loose, 1 == strict, default == 0
; loose == Names with no characters or just whitespace are considered blank.
; strict == Only names with no characters are considered blank.
blank_name_algorithm: 0
